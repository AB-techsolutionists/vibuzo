# deprecated-commands-cleanup — Cleaned up deprecated command files

*Paste opencode /compact output below this line*
────────────────────────────────────────
## Goal
Delete the 4 deprecated command files (`implement.md`, `plan.md`, `tasks.md`, `review.md`) and remove all active references to them across the codebase.

## Constraints & Preferences
- Approval level 3 (Full Control) — every action requires approval gate
- Deprecated files kept but marked (not deleted) per earlier session constraint — **overridden** by this session's decision to permanently delete
- Approval gate must be rendered inside a code block (triple backticks) so opencode displays it as a terminal card UI
- `context/index.md` must be updated after every context file change (hard rule)

## Progress
### Done
- Converted README roadmap versions from `v0.x` → `v0.0.x` format, capped patch at 9 (`v0.0.10` → `v0.1.0`)
- Deleted 4 files: `commands/implement.md`, `commands/plan.md`, `commands/tasks.md`, `commands/review.md`
- Updated `agents/vibuzo.md` — description, table, delegation reference, gate rule all changed from `/implement` → `/spec`
- Updated `agents/deepveloper.md` — description changed from "triggered only by /implement" → "triggered only as a subtask via /spec"
- Updated `AGENTS.md` — 6 references cleaned (trigger mechanism, delegation options, approval level table)
- Updated `README.md` — 4 references cleaned (tagline, agent roles, approval level table, roadmap entry)
- Updated `context/architecture/agent-restructure.md` — 5 references cleaned (diagram, roles, delegation language)
- Updated `context/standards/source-mirror-synchronization.md` — removed `implement.md` from active table, removed `plan.md`/`tasks.md`/`review.md` from deprecated table
- Added code block rendering instruction to `agents/vibuzo.md`, `agents/deepveloper.md`, `context/architecture/approval-gates.md`
- Updated `context/index.md` — agent-restructure description and source-mirror description

### Done (continued)
- Changed Orchestrator's `mode: primary` → `mode: subagent` + `hidden: true` in both copies (agents/ and .opencode/agent/core/)
- Created `opencode.jsonc` at project root — overrides built-in `build` agent with Vibuzo's config (permissions, prompt, temperature)
- Created `.opencode/prompts/vibuzo.txt` — clean system prompt extracted from vibuzo.md body (no YAML frontmatter)

### In Progress
- (none)

### Blocked
- (none)

## Key Decisions
- Delete deprecated command files permanently (overrides prior "keep but mark" policy)
- Always render approval gate inside a triple-backtick code block so opencode renders it as a terminal card UI element
- Historical files (`specs/`, `context/sessions/`) keep old references as records — not cleaned
- Override built-in `build` agent in `opencode.jsonc` so `/new` starts with Vibuzo-like behavior
- Orchestrator demoted from `primary` to `subagent` + `hidden` so it doesn't compete in Tab rotation
- Use a clean `.opencode/prompts/*.txt` file for prompt overrides instead of referencing markdown files with YAML frontmatter

## Next Steps
- Address remaining `/implement`/`/plan`/`/tasks`/`/review` references in `context/architecture/spec-command.md` (deprecation mapping table — still accurate as historical record, may want update)
- Verify installer scripts (`install.sh`, `install.ps1`) no longer reference deleted files

## Critical Context
- Approval level is 3 (Full Control) — every action requires gate approval
- `commands/spec.md` still references `plan.md`, `tasks.md`, `review.md` as **output artifacts** (files `/spec` creates in `specs/<feature>/`) — these are NOT command references and should be kept
- `.opencode/commands/` mirror copies of deleted files didn't exist locally (generated by installer) — no cleanup needed there
- The approval gate format works only when inside triple backticks — plain text rendering loses the terminal card UI

## Relevant Files
- `commands/implement.md` — DELETED (was deprecated `/implement` command)
- `commands/plan.md` — DELETED (was deprecated `/plan` command)
- `commands/tasks.md` — DELETED (was deprecated `/tasks` command)
- `commands/review.md` — DELETED (was deprecated `/review` command)
- `agents/vibuzo.md` — Updated: mandates code block for approval gate, references `/spec` instead of `/implement`
- `agents/deepveloper.md` — Updated: description and code block rendering instruction
- `AGENTS.md` — Updated: 6 references cleaned
- `README.md` — Updated: 4 references cleaned, roadmap uses `v0.0.x`/`v0.1.0`
- `context/architecture/approval-gates.md` — Updated: added code block rendering requirement
- `context/architecture/agent-restructure.md` — Updated: `/implement` → `/spec` references
- `context/standards/source-mirror-synchronization.md` — Updated: removed deleted files from tables
- `context/index.md` — Updated: descriptions for agent-restructure and source-mirror entries
- `context/architecture/spec-command.md` — Still has historical deprecation mapping table referencing deleted commands (pending review)
- `context/sessions/2026-06-05-codebase-audit.md` — Historical record, not changed
- `agents/orchestrator.md` — Changed `mode: primary` → `mode: subagent` + `hidden: true`
- `.opencode/agent/core/orchestrator.md` — Same mode change (mirror copy)
- `opencode.jsonc` (NEW) — Project-level config, overrides built-in `build` agent with Vibuzo
- `.opencode/prompts/vibuzo.txt` (NEW) — Clean system prompt for build agent override
────────────────────────────────────────
*Paste opencode /compact output above this line*

## Relevant Files
- `commands/implement.md`: Deleted — deprecated in favor of `/spec`
- `commands/plan.md`: Deleted — deprecated in favor of `/spec`
- `commands/tasks.md`: Deleted — deprecated in favor of `/spec`
- `commands/review.md`: Deleted — deprecated in favor of `/spec`
- `AGENTS.md`: Removed all `/implement`, `/plan`, `/tasks`, `/review` references
- `README.md`: Updated roadmap to semver v0.0.x, removed old command references
- `agents/vibuzo.md`: Replaced `/implement` delegation with `/spec`, added code-block rule for approval gates
- `agents/deepveloper.md`: Updated description from `/implement` subtask to `/spec` delegation, added code-block rule
- `context/architecture/agent-restructure.md`: Updated `/implement` references to `/spec`
- `context/architecture/approval-gates.md`: Added code block rendering instruction for terminal card UI
- `context/standards/source-mirror-synchronization.md`: Removed deleted files from mirror table
- `context/index.md`: Updated descriptions for changed context files
- `agents/orchestrator.md`: Deprecated — demoted from `primary` to `subagent` + `hidden`
- `.opencode/agent/core/orchestrator.md`: Mirror copy of same change
- `opencode.jsonc`: Project-level config, overrides `build` agent with Vibuzo prompt/permissions
- `.opencode/prompts/vibuzo.txt`: Clean prompt file for build agent override

## Timeline
| 2026-06-05 | 03:04 | `deprecated-commands-cleanup` | Cleaned up deprecated commands |
| 2026-06-05 | 03:14 | `deprecated-commands-cleanup` | Fixed /new to default to Vibuzo |
